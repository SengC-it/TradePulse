# M3-R4-D.1 Frozen Gate Applicator

Status: IMPLEMENTATION FREEZE ONLY

Attempt #7 is the only authoritative Round-004 performance result. The
performance lock is `FIRST_M3_R4_PERFORMANCE_RESULT_GENERATED` and is
TRIGGERED; Round-004 remains VALID.

M3-R4-D.1 adds only an offline, deterministic applicator for the already
frozen Round-004 Gate. It validates the canonical Gate and Plan, the raw
summary/audit/results provenance, F1-F6 validation construction, candidate
identity hashes, and every frozen gate before applying the frozen tie rule.

No performance rerun occurred. No Gate, Plan, formula, fold, cost,
settlement, sample-floor, tie-rule, or candidate definition changed. No
real-evidence authoritative gate application occurred in D.1, and the
reserved selection outputs are not created by this milestone.

`baseline-002` remains `NOT_FROZEN`; M3-J remains `BLOCKED`; M4 remains
`NOT_STARTED`.
